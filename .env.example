# Copy to `.env` in the project root (loaded by server/server.js).
MONGO_URI=mongodb://127.0.0.1:27017/blockestate
PORT=4000

# Optional — only if you use Cloudinary uploads
# CLOUDINARY_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
